# Security

Do not commit real Power BI workspace exports, tenant IDs, dataset IDs, source queries, customer data, row-level security mappings, access tokens, credentials, screenshots, or production dashboard evidence.

Use synthetic metric metadata only for public portfolio proof.
